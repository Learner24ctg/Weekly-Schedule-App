import React from "react";
import ReactDOM from "react-dom";
import "./index.css"; // Optional: for styling
import ScheduleApp from "./App";

ReactDOM.render(
  <React.StrictMode>
    <ScheduleApp />
  </React.StrictMode>,
  document.getElementById("root")
);
